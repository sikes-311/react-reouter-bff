server:
  port: 8080

spring:
  application:
    name: my-app

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

downstream:
  base-url: ${DOWNSTREAM_BASE_URL:http://localhost:8081}

management:
  endpoints:
    web:
      exposure:
        include: health
